# streamlit==1.51.0
# tiktoken==0.12.0
# langchain==1.0.3
# langchain-community==0.4.1
# langchain-core==1.0.3
# langchain-openai==1.0.2
# langchain-pinecone==0.2.13
# openai==2.7.1
# langchain-cohere

# LangChain 0.1.x releases expose ContextualCompressionRetriever via
# langchain.retrievers.contextual_compression and keep the split-core packages.
# We stay within the 0.1 line to avoid the breaking 0.2 refactor for now.
# langchain>=0.1.17,<0.2.0
# langchain-community>=0.1.0,<0.2.0
# langchain-core>=0.1.52,<0.2.0
# langchain-openai>=0.0.8,<0.1.0
# langchain-pinecone>=0.0.3,<0.1.0
# langchain-cohere>=0.0.6,<0.1.0


# LangChain 0.3.x exposes ContextualCompressionRetriever at
# langchain.retrievers.contextual_compression without relying on the
# compatibility layer.  Keep the companion packages on matching minor versions.
# langchain>=0.3.14,<0.4.0
# langchain-community>=0.3.12,<0.4.0
# langchain-core>=0.3.25,<0.4.0
# langchain-openai>=0.2.5,<0.3.0
# langchain-pinecone>=0.1.1,<0.2.0
# langchain-cohere>=0.3.0,<0.4.0

# langchain-openai 0.2.x targets the OpenAI Python client 1.x series.
# openai>=1.60.0,<2.0.0


# langchain-openai 0.0.x still targets the OpenAI Python client 1.x series.
# openai>=1.10.0,<2.0.0
# openai==2.7.1


# Hier die aktuellsten Versionen der Pakete (Stand: heute, 11.11.2025, laut PyPI):
# langchain → 1.0.5 
# langchain-community → 0.4.1 
# langchain-core → 1.0.4 
# langchain-openai → 1.0.2 
# langchain-pinecone → 0.2.13 
# langchain-cohere → 0.5.0
# openai==2.7.1

streamlit==1.51.0
tiktoken==0.12.0
# LangChain 1.0.x again exposes ContextualCompressionRetriever directly under
# langchain.retrievers.contextual_compression.  Keep the ecosystem packages on
# the matching major/minor releases so their public APIs remain compatible.
langchain==1.0.5
langchain-community==0.4.1
langchain-core==1.0.4
langchain-openai==1.0.2
langchain-pinecone==0.2.13
langchain-cohere==0.5.0


# langchain-openai 1.0.x continues to require the "classic" OpenAI Python
# client (<2).  Pin a known-good release so pip resolves without conflicts when
# installing alongside the rest of the stack.
openai==2.7.1
# openai==1.61.1

